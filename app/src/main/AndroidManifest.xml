<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.ufrpe.bsi.bianca.crudjava">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".pessoa.gui.CadastroPessoa">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".pessoa.gui.TelaInicial" />
        <activity android:name=".endereco.gui.ListarEnderecos" />
        <activity android:name=".endereco.gui.CadastroEndereco" />
        <activity android:name=".endereco.gui.EditarEndereco"></activity>
    </application>

</manifest>